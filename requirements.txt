# =======================================================
# متطلبات المشروع الأساسية لوحدات الإشعار وقاعدة البيانات
# =======================================================

# 1. ORM والتعامل مع قاعدة البيانات
SQLAlchemy==2.0.23      # المكتبة الرئيسية للتفاعل مع قواعد البيانات.

# 2. مشغل قاعدة البيانات (اختر المشغل الذي تستخدمه فعلياً)
psycopg2-binary==2.9.9  # مشغل PostgreSQL (إذا كنت تستخدم Postgres)
# OR
# pymysql==1.1.0        # مشغل MySQL (إذا كنت تستخدم MySQL/MariaDB)

# 3. إدارة الإعدادات والبيئة
python-dotenv==1.0.1    # لقراءة متغيرات البيئة من ملف .env (مهم للأمان).

# =======================================================
# متطلبات أخرى قد تحتاج إليها (حسب سياق مشروعك)
# =======================================================

# requests==2.31.0      # إذا كنت ستحتاج إلى إجراء طلبات HTTP (مثل جلب البيانات من API).